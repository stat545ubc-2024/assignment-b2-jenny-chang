% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_unique.R
\name{count_unique}
\alias{count_unique}
\title{Count Unique Values by Group}
\usage{
count_unique(data, summ_var, ..., .groups = "drop")
}
\arguments{
\item{data}{A data frame or tibble containing the data to be summarized. Chose \code{data} as it clearly indicates the primary data input.}

\item{summ_var}{The variable for which unique values are counted within each group. Named \code{summ_var} to clearly denote that this is the summarized variable in the count.}

\item{...}{One or more grouping variables within the dataset. These variables define the grouping structure for summarizing \code{summ_var}. The \code{...} notation allows for flexible input of single or multiple variables.}

\item{.groups}{A character string indicating how to handle grouping in the result. Options are "drop" (default), "keep", "drop_last", or "rowwise". The \code{.groups} name follows \code{dplyr} syntax for familiarity.}
}
\value{
A tibble with one row per group and a column \code{unique_sum}, showing the count of unique values in \code{summ_var} for each group defined by \code{...}.
}
\description{
This function calculates the number of unique values in a specified column (\code{summ_var}) within each group defined by one or more grouping variables. It is useful for understanding the distribution of unique values within groups.
}
\examples{
data <- tibble::tibble(
  category = c("A", "A", "B", "B", "C"),
  subgroup = c(1, 1, 2, 2, 1),
  value = c(10, 20, 10, NA, 30)
)

# Count unique values in `value` by `category`
count_unique(data, summ_var = value, category)

# Count unique values in `value` by both `category` and `subgroup`
count_unique(data, summ_var = value, category, subgroup)
}
